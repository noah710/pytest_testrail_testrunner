First step is to cache the test scripts so that each corresponds to a case ID. This is manual

Once cases are added, they can be added to runs in testrail online GUI. This script will get the cases in each run and run them.

IMPORTANT: Case ID's must be unique and must match
